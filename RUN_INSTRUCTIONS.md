# Reproduce the practical results

1. Place `dnn dataset.zip` in `C:/Users/shana/Downloads/`.
2. Run:

```bash
python src/train_experiments.py
```

The script builds labels, trains the baseline and five variations, and saves the results table and loss curves in `results/`.
